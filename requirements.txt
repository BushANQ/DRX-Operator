# DRX-Operator 核心依赖

# TUI 框架（8.x 实测；selection/Screen API 依赖 8.x）
textual>=8.0,<9

# 富文本渲染（chat_panel/footer/banner 直接 import）
rich>=13.0.0

# Web 搜索（ddgs 为主，失败时回退 DuckDuckGo Instant Answer API）
ddgs>=9.0.0

# HTTP 请求（openai/anthropic 的传递依赖，显式声明）
requests>=2.28.0
urllib3>=1.26.0

# LLM Providers（Anthropic / OpenAI / DeepSeek 均经 OpenAI 兼容客户端）
anthropic>=0.18.0
openai>=1.0.0

# Skills 配置解析（skills/*/skill.yaml）
pyyaml>=6.0
